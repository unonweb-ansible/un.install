---
- name: systemctl --system daemon-reload
  become: true
  ansible.builtin.systemd_service:
    daemon_reload: true
    scope: system

- name: update-desktop-database
  become: true
  ansible.builtin.command: "update-desktop-database {{ un_install_desktop_dst | dirname }}"